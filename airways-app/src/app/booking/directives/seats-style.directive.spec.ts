import { SeatsStyleDirective } from './seats-style.directive';

describe('SeatsStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new SeatsStyleDirective();
    expect(directive).toBeTruthy();
  });
});
